Phoom Saleewong 1660903129
khemmathat klindong 1660902543

# 🎥 Kaggle Data Pipeline: The Ultimate 1Million Movies Dataset (TMDB + IMDb)

[cite_start]โปรเจกต์นี้คือการสร้าง **Automated Data Pipeline** แบบครบวงจร เพื่อดึงข้อมูลภาพยนตร์ขนาดใหญ่ (มากกว่า 1 ล้านแถว [cite: 6][cite_start]) จาก Kaggle [cite: 3][cite_start], จัดเก็บใน PostgreSQL [cite: 57][cite_start], แปลงข้อมูลด้วย Pandas [cite: 58][cite_start], และเผยแพร่ผลลัพธ์สุดท้ายไปยัง Looker Studio ผ่าน Google Sheets [cite: 59, 61]

## 🎯 วัตถุประสงค์และ Insight

[cite_start]เป้าหมายหลักคือการวิเคราะห์แนวโน้มอุตสาหกรรมภาพยนตร์ทั่วโลก [cite: 13] [cite_start]และศึกษาความสัมพันธ์ของตัวแปรสำคัญ (เช่น งบประมาณ, รายได้, คะแนนรีวิว) [cite: 14]

### ❓ คำถามเชิงธุรกิจหลัก (Business Questions)

[cite_start]Dashboard ที่สร้างขึ้นจะถูกออกแบบมาเพื่อตอบคำถามเหล่านี้ (5 Visualization [cite: 99]):

1.  [cite_start]ประเภทภาพยนตร์ (**Genres**) ใดทำรายได้เฉลี่ยสูงที่สุด? [cite: 40]
2.  [cite_start]**งบประมาณ**การสร้างภาพยนตร์มีความสัมพันธ์กับ**รายได้**หรือไม่? [cite: 41]
3.  [cite_start]แนวโน้มจำนวนภาพยนตร์ที่ผลิตในแต่ละปีเป็นอย่างไร? [cite: 42]
4.  [cite_start]ประเทศผู้ผลิตใดสร้างภาพยนตร์ที่ได้รับความนิยมมากที่สุด? [cite: 43]
5.  [cite_start]ภาพยนตร์ที่มี **Runtime** ยาวกว่า มักได้คะแนนสูงกว่าหรือไม่? [cite: 44]

***

## ⚙️ สถาปัตยกรรมและเทคโนโลยี (Architecture & Tech Stack)

| ขั้นตอน | สคริปต์ | เครื่องมือ/เทคโนโลยี | หน้าที่ |
| :--- | :--- | :--- | :--- |
| **Source** | N/A | [cite_start]Kaggle Dataset [cite: 55] (TMDB + IMDb) [cite_start][cite: 2] | แหล่งข้อมูลภาพยนตร์กว่า 1 ล้านเรื่อง [cite: 6] |
| **Ingestion & Load** | `ingest.py` [cite: 70] | Python (Pandas, SQLAlchemy) [cite: 56] | ดึงข้อมูลดิบเข้า Schema `raw_data` ใน PostgreSQL [cite: 71] |
| **Transformation** | [cite_start]`transform.py` [cite: 73] | [cite_start]Python (Pandas) [cite: 74] | [cite_start]ทำความสะอาดข้อมูล [cite: 76] [cite_start](เช่น จัดการ Missing Values [cite: 37]) และสร้างตารางใน Schema `production` [cite: 81, 82] |
| **Publish** | `publish.py` [cite: 85] | Python (gspread) [cite: 87] | ส่งข้อมูลจาก `production` ขึ้น **Google Sheets** (Cloud Staging) [cite: 87] |
| **Automation** | `run_pipeline.py` [cite: 90] | Python | ควบคุมการรัน `ingest`, `transform`, `publish` ต่อเนื่องกัน [cite: 92] |
| **Visualization** | N/A | **Looker Studio** [cite: 95] | สร้าง Dashboard เพื่อวิเคราะห์ Insight [cite: 99] |

***

## 🚀 วิธีการติดตั้งและรันโปรเจกต์ (How to Run)

โปรเจกต์นี้ทำงานบนสภาพแวดล้อม Local โดยใช้ Docker สำหรับ Database [cite: 57]

### 1. การติดตั้ง Prerequisites

* ติดตั้ง Docker และ Docker Compose
* ติดตั้ง Python (แนะนำ 3.8+)
* ติดตั้ง Dependencies ใน Python โดยใช้ไฟล์ `requirements.txt`:
    ```bash
    pip install -r requirements.txt
    ```

### 2. การตั้งค่า Credentials

1.  **สร้างไฟล์ `.env`:** สร้างไฟล์ชื่อ `.env` ใน Root Directory และกำหนดค่าการเชื่อมต่อ PostgreSQL:
    ```env
    DB_HOST=localhost
    DB_PORT=5432
    POSTGRES_USER=myuser
    POSTGRES_PASSWORD=mypassword
    POSTGRES_DB=mydatabase
    ```
2.  **ตั้งค่า Google Sheets API:**
    * [cite_start]สร้าง Service Account และดาวน์โหลดไฟล์ JSON Credentials [cite: 107]
    * บันทึกไฟล์ JSON ไว้ใน Root Directory และตั้งชื่อไฟล์เป็น **`client_secret.json`**
    * **สำคัญ:** ห้าม Commit ไฟล์ **`client_secret.json`** และไฟล์ **`.env`** ขึ้น GitHub โดยเด็ดขาด [cite: 111]
    * แชร์สิทธิ์ **Editor** ของ Google Spreadsheet ปลายทางให้กับอีเมล Service Account นั้น [cite: 107]

### 3. รัน Database Container

ใช้ Docker Compose เพื่อรัน PostgreSQL Database ในพื้นหลัง:

```bash
docker-compose up -d

### 4. รัน Pipeline อัตโนมัติ (Automated Run)
ใช้สคริปต์หลัก run_pipeline.py เพื่อดำเนินการทุกขั้นตอนตั้งแต่ Ingest จนถึง Publish 